{
  "name": "feed-ticket-purchase-bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run generate && tsc",
    "start": "node dist/index.js",
    "dev": "npm run generate && tsx src/index.ts",
    "generate": "cd .. && forge build && cd - && wagmi generate",
    "test": "npm run generate && node --import tsx --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@wagmi/cli": "2.10.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "viem": "^2.51.3"
  }
}
